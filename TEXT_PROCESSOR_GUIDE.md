# TextProcessor 页面功能说明

## 页面功能

### 1. 两栏布局 ✅
页面采用了左右两栏的布局设计：

**左边框 - 原始文本输入区**
- 蓝色标题栏，显示"原始文本"和字数统计
- 用户可以在此粘贴需要处理的文本
- 实时显示输入字数
- 处理时会显示为半透明状态

**右边框 - 处理结果展示区**
- 绿色标题栏，显示"处理结果"
- 处理完成后会显示"复制"按钮
- 实时显示AI处理后的文本（流式输出）
- 未处理时显示提示信息和图标

### 2. 用户流程控制 ✅

#### 点击"开始降AI率"按钮后的流程：

**步骤1: 检查登录状态**
- ❌ 未登录 → 弹出登录/注册弹窗
- ✅ 已登录 → 进入步骤2

**步骤2: 检查字数和剩余额度**
- 调用后端 API `/api/process/check` 检查
- 显示计费字数和费用
- 检查剩余字数是否足够

**步骤3: 判断处理方式**
- ✅ 首次用户且满足500字体验 → 直接开始处理
- ❌ 字数不足 → 显示错误提示，1.5秒后自动跳转到价格方案页面
- ✅ 字数足够 → 开始AI处理

### 3. 登录/注册弹窗 ✅

弹窗功能：
- 邮箱验证码登录
- 首次登录即自动注册
- 新用户免费体验500字
- 开发环境显示验证码（方便测试）

### 4. 邮件验证码服务 ✅

使用 **MailerSend** 发送验证码邮件：
- 精美的 HTML 邮件模板，包含品牌样式
- 6位随机验证码，5分钟有效期
- 开发环境支持控制台查看验证码
- API 调用失败时的降级处理机制

**邮件内容包含：**
- 品牌头部（渐变紫色背景）
- 大号字体显示验证码
- 有效期说明和安全提示
- 品牌宣传语和联系方式

### 5. 实时字数统计 ✅

顶部状态栏显示：
- 已输入字数
- 计费字数（最低500字）
- 处理费用

### 6. 流式输出 ✅

- AI处理时实时显示处理进度
- 右侧边框逐步显示处理结果
- 处理完成后显示"复制"按钮

### 7. 用户信息展示 ✅

导航栏显示：
- 用户邮箱
- 剩余字数
- 充值按钮

## API 集成

### 使用的 API 接口

1. **用户信息**
   - `authApi.getMe()` - 获取当前用户信息

2. **发送验证码**
   - `authApi.sendCode(email)` - 发送邮箱验证码

3. **登录**
   - `authApi.login(email, code)` - 验证码登录

4. **检查字数**
   - `processApi.check(text)` - 检查字数和费用

5. **处理文本**
   - `processApi.process(text, onChunk, onComplete)` - 流式处理文本

## 页面样式

### 颜色方案
- 主色调：蓝色 (#2563EB)
- 成功色：绿色 (#16A34A)
- 背景色：渐变 (slate-50 → slate-100)

### 动画效果
- 页面加载动画（淡入 + 上移）
- 两栏分别从左右滑入
- 按钮悬停和点击动画
- 处理中脉冲动画

### 响应式设计
- 大屏：两栏并排（lg:grid-cols-2）
- 小屏：单栏堆叠（grid-cols-1）

## 服务说明

页面底部展示4个服务特点：
1. 计费规则 - 每字0.012元，500字起步
2. 质量保证 - 未通过检测率，全额退款
3. AI算法 - 反AI独家算法
4. 支持平台 - 中国知网、维普、Turnitin

## 使用流程示例

### 场景1: 首次用户
1. 访问页面 → 点击"开始降AI率"
2. 弹出登录窗口 → 输入邮箱和验证码
3. 自动注册并获得500字免费额度
4. 粘贴文本 → 点击"开始降AI率"
5. 实时看到处理结果 → 复制使用

### 场景2: 老用户（字数不足）
1. 访问页面 → 已登录
2. 粘贴文本 → 点击"开始降AI率"
3. 提示字数不足 → 自动跳转到价格页面
4. 选择套餐 → 充值成功
5. 返回页面 → 重新处理文本

### 场景3: 老用户（字数充足）
1. 访问页面 → 已登录
2. 粘贴文本 → 点击"开始降AI率"
3. 实时看到处理结果 → 复制使用
4. 字数自动扣除

## 技术特点

1. **前后端分离**
   - 使用 API 客户端与后端通信
   - 所有请求携带 Cookie 进行身份验证

2. **流式输出**
   - 使用 Server-Sent Events (SSE)
   - 实时显示AI处理进度
   - 提升用户体验

3. **错误处理**
   - 友好的错误提示
   - 自动重试机制
   - 失败自动退款（字数退还）

4. **用户体验**
   - 登录状态持久化
   - 实时字数统计
   - 一键复制功能
   - 响应式布局

## 访问地址

前端: http://localhost:5000
后端: http://localhost:5001

## 测试建议

1. 测试登录/注册流程
2. 测试首次用户500字免费体验
3. 测试字数不足时跳转到价格页面
4. 测试AI文本处理的流式输出
5. 测试复制功能
6. 测试清空功能
